# Unused when railway.toml uses builder = DOCKERFILE (preferred).
# Nixpacks (Railway build). Next 16 + AWS SDK v3 need Node 20+.
# https://nixpacks.com/docs/configuration/file
[variables]
NIXPACKS_NODE_VERSION = "22"
